overwrite: true
schema: ./src/graphql/schema.graphql
documents: "**/*!(*.generated).{gql,graphql}"
generates:
  src/graphql/graphQlApiHooks.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
    config:
      withHooks: true
